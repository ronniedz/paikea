/**
* @Author: gajo
* @Date:   2016-06-24T09:28:26-07:00
* @Last modified by:   gajo
* @Last modified time: 2016-10-31T00:33:53-07:00
*/

import PlaylistsList from '../index'

// import expect from 'expect'
import { mount } from 'enzyme'
import React from 'react'
import { IntlProvider } from 'react-intl'

describe('<PlaylistsList />', () => {
  it('should render the listwrap parent', () => {
    // const renderedComponent = mount(
    //   <IntlProvider locale="en">
    //     <PlaylistsList />
    //   </IntlProvider>
    // )

    // there should be only one div wrapper, which is the shell for the unordered list videos
    // expect(renderedComponent.name()).toEqual('div')
  })
})
